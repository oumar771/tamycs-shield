describe('Categories', () => {
  test('should return default categories', () => {
    expect(true).toBe(true);
  });

  test('should create custom category', () => {
    expect(true).toBe(true);
  });
});
